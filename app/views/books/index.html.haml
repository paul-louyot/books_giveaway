%h1 Books

%table
  - @books.each do |book|
    %tr
      %td
        = book.title
      %td
        = book.author
      %td
        = render partial: "form", locals: { book: book, user_name: @user_name }


