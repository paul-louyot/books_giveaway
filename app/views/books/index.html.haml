%h1 Books#index
%p Find me in app/views/books/index.html.erb

%table
  - @books.each do |book|
    %tr
      %td
        = book.title
      %td
        = book.author
      %td
        - if book.is_reserved?
          - if book.user_name == @user_name
            = form_with model: book do |form|
              = form.hidden_field :user_name, value: nil
              = form.submit "unreserve"
          - else
            book already reserved

        - else
          = form_with model: book do |form|
            = form.hidden_field :user_name, value: @user_name
            = form.submit "reserve book"


